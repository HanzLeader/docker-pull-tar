{
  "name": "docker-pull-tar",
  "version": "2.0.0",
  "private": true,
  "description": "Docker 镜像下载",
  "scripts": {
    "dev": "pnpm --filter frontend electron:dev",
    "build": "pnpm --filter frontend electron:build",
    "start": "frontend/dist-electron/win-unpacked/DockerPull.exe"
  }
}