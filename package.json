{
  "name": "docker-pull-tar",
  "version": "2.0.0",
  "private": true,
  "description": "Docker 镜像下载",
  "scripts": {
    "electron:dev": "pnpm --filter frontend electron:dev",
    "electron:build": "pnpm --filter frontend electron:build",
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "build:backend": "python -m PyInstaller backend.spec --clean --noconfirm && node -e \"const fs=require('fs');if(!fs.existsSync('backend-dist'))fs.mkdirSync('backend-dist');fs.copyFileSync('dist/backend.exe','backend-dist/backend.exe')\"",
    "build:full": "npm run build:backend && cd frontend && npm run electron:compile && npm run build && npx electron-builder --config electron-builder.json && cd ..",
    "build:frontend": "cd frontend && npm run build && npm run asar:update-frontend && cd ..",
    "build:electron": "cd frontend && npm run electron:compile && npm run asar:update-electron && cd ..",
    "kill": "taskkill /F /IM DockerPull.exe & taskkill /F /IM backend.exe"
  }
}